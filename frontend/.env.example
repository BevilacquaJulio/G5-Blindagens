# URL base da API NestJS (inclua o prefixo /api). Baked no build — nunca coloque segredo aqui.
VITE_API_URL=http://localhost:3000/api

# Credenciais demo na tela de login (portfolio). Opcional — omita em produção real.
VITE_DEMO_ADMIN_EMAIL=admin@atlas.com
VITE_DEMO_ADMIN_PASSWORD=atlas_stock123
